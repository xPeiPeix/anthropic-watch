# Keep this directory in version control.
# Glance validates server.assets-path exists at startup; on a clean checkout
# the watcher has not run yet, so this placeholder ensures the dashboard can
# start. The generated anthropic_quota.json here is git-ignored.
